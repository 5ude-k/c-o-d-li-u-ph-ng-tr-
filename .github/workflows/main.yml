name: Run Scraper (Excel → SQL)

on:
  schedule:
    - cron: "0 17 * * *"  # chạy hằng ngày lúc 17h UTC (0h Việt Nam)
  workflow_dispatch:
  push:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # --- 1️⃣ Clone repo ---
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      # --- 2️⃣ Cài Python ---
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # --- 3️⃣ Cài thư viện cần thiết ---
      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 nbconvert jupyter pandas openpyxl sqlite-utils

      # --- 4️⃣ Chạy scraper ---
      - name: Convert and run notebook
        run: |
          jupyter nbconvert --to python API.ipynb
          python API.py |& tee log.txt

      # --- 5️⃣ Tạo file .sql trực tiếp từ Excel ---
      - name: Generate SQL dump (UTF-8 from Excel)
        run: |
          python - <<'EOF'
          import pandas as pd, sqlite3, os

          excel_file = "nhatot_phongtro_danang_all.xlsx"
          sql_file = "data.sql"

          if not os.path.exists(excel_file):
              print(f"❌ Không tìm thấy file {excel_file}")
              exit(1)

          print(f"🔄 Đang tạo {sql_file} từ {excel_file}...")

          # Đọc Excel giữ nguyên tiếng Việt
          df = pd.read_excel(excel_file, dtype=str)

          # Tạo database SQLite tạm
          conn = sqlite3.connect("temp.db")
          df.to_sql("nhatot_phongtro_danang", conn, if_exists="replace", index=False)

          # Xuất ra file SQL (UTF-8, không lỗi font)
          with open(sql_file, "w", encoding="utf-8-sig") as f:
              for line in conn.iterdump():
                  f.write(f"{line.strip()}\n")

          conn.close()
          os.remove("temp.db")
          print("✅ Đã tạo file data.sql (UTF-8) thành công!")
          EOF

      # --- 6️⃣ Nén kết quả ---
      - name: Compress result files
        run: |
          zip -r data.zip nhatot_phongtro_danang_all.xlsx data.sql log.txt
          echo "✅ Đã nén toàn bộ thành data.zip"

      # --- 7️⃣ Commit kết quả ---
      - name: Commit result files
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add nhatot_phongtro_danang_all.xlsx data.sql log.txt data.zip
          git commit -m "Auto scrape + SQL (from Excel) $(date +'%Y-%m-%d %H:%M:%S')" || echo "No changes"
          git push
